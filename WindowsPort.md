# Windows 移植版（SayAllWin）测试手册

- 适用版本：仓库 `Windows/SayAllWin/` 当前源码构建的 `SayAll.exe`（v0.2.0-beta，未签名 Debug 构建）。
- 测试前准备：
  - Windows 10 19041+，64 位；已安装 .NET SDK 10 并完成 `dotnet build`（或使用交付的便携版目录）。
  - 机器具备 BLE 蓝牙适配器；小米蓝牙遥控器 2 Pro（RC003）已在系统蓝牙中配对（长按主页+菜单至指示灯快闪）。
  - 安装 VB-CABLE 并重启；目标验证应用任选其一：记事本+输入法语音输入、微信/钉钉语音输入、任意录音工具。
  - 日志收集：`%LOCALAPPDATA%\SayAll\logs\runtime.log`（复现问题后打包整个 logs 目录；日志不含语音内容与蓝牙地址）。
- 自动化边界：本手册的 BLE/语音闭环用例需要真实遥控器与真实虚拟声卡，自动化只能覆盖构建与进程冒烟（已执行：构建 0 错误、启动 5 秒存活、日志写入、BLE 进入 Scanning 状态）。

## 用例 1：构建与首次启动（已自动化通过）

1. 运行 `Windows\build.ps1`。
   - 预期：`dotnet build` 0 错误；输出 `SayAll.exe` 路径。
   - 失败判定：编译错误、缺少 `SayAll.exe`。
2. 双击 `SayAll.exe`。
   - 预期：出现托盘麦克风图标与设置窗口，顶部状态从「搜索中」推进；日志出现 `LOG START`、`BLE SCANNING`。
   - 失败判定：进程 5 秒内退出、无托盘图标、日志为空或报 FATAL。
3. 关闭设置窗口后再次从托盘双击打开。
   - 预期：窗口重新显示，配置不丢失。

## 用例 2：虚拟麦克风链路（无遥控器，可先行）

1. 设置 →「连接与语音」，确认设备列表出现带 ★ 的 `CABLE Input` 并选中。
2. 目标应用输入设备选择 `CABLE Output`。
3. 点「发送 1 秒测试音」。
   - 预期：目标应用电平表跳动（440Hz 一秒）；无声音则失败。
   - 失败判定：按钮弹窗提示失败、电平表无反应、`AUDIO` 相关错误日志。
4. 修改增益滑块（±24 dB）后重复测试音。
   - 预期：音量随增益变化；重启后设置保留。

## 用例 3：连接遥控器（需真实遥控器）

1. 遥控器开机并进入连接范围；在系统蓝牙中保持已配对/已连接。
2. 托盘菜单「重新连接」。
   - 预期：状态推进为「正在连接 → 正在初始化语音通道 → 已连接（设备名）」，显示电量；日志出现 `BLE CONNECTING source=paired_device`、`BLE CONNECTED`、`BATTERY level=<n>`、`MODEL identified=Rc003`、`ATVV CAPS`、`BLE READY`。
   - 失败判定：30 秒后仍无连接且日志出现连续 `BLE CONNECT FAILED`，或 `voice_channel_discovery_failed` 连续 3 次以上不恢复（偶发 1 次后由重连策略自动恢复属正常）。
   - 2026-09-07 真机验收（RC003 / Windows 11）：连接、服务发现、电量 99%、型号识别、ATVV 能力协商（version=256 codec=2 frame=120 16kHz）、UI 绿点"已连接"全部通过；修复记录见 `Bugs/2026-09-07-windows-ble-paired-flag-filter.md`。语音闭环仍待用户实测（用例 4）。
   - 注入键说明：语音键注入 **F13**（0x7C），或选「微信输入法按住说话」模式注入 Ctrl+Win；遥控器 keyboard 键位（F5/方向/OK/Back/Home/Menu）的原生事件由低级键盘钩子在遥控器活跃期（最近 30 秒内有遥控器操作）硬拦截（对应 macOS 设备独占语义），同期物理键盘同键位也被吞（浏览器刷新用 Ctrl+R 替代），闲置 30 秒后恢复。音量/电源/TV 等 consumer page 键不在 Raw Input 键盘通道内，保持系统直通。Windows 对键盘 HID collection 强制独占（ReadFile 报拒绝访问），按键报告源为 **Raw Input**（RIDEV_INPUTSINK）而非 ReadFile。修复记录见 `Bugs/2026-09-07-windows-voice-key-f5-repeat-leak.md`。使用方语音工具需把触发键设为 F13（微信输入法用 Ctrl+Win 模式）。
3. 关闭遥控器电源等待 30 秒再开机。
   - 预期：应用自动重连（指数退避 3s 起步）；日志出现 `BLE RECONNECT scheduled`。
4. 系统睡眠唤醒。
   - 预期：唤醒后触发 `BLE WAKE recovery_requested` 并自动重连。

## 用例 4：语音会话闭环（需真实遥控器 + 虚拟声卡）

1. 连接就绪后，目标应用输入设备选 `CABLE Output`，聚焦其输入框。
   - 链路预验证：点「发送测试音」，在系统声音设置的输入面板（选 CABLE Output）应看到电平跳动。2026-09-07 已验证：`AUDIO TESTTONE queued`，采集 CABLE Output `peak=0.1499`（修复记录见 `Bugs/2026-09-07-windows-audio-render-format.md`）。
2. 按住遥控器语音键说一句话，松开。
   - 预期：按住期间目标应用收到连续音频（电平表/文字上屏）；松开后 1 秒内停止；说话内容在目标应用可被识别；日志出现 `HID DEVICE connected source=rawinput`（首次按键）与 `HID VOICEKEY down f5_total=<n>`。
   - 失败判定：无音频、音频断续超过 2 秒、松开后仍持续输出、按语音键时浏览器/前台应用触发刷新（F5 泄漏）；若应用跟随"默认输入"却无声，先确认默认输入是否被 VoiceMeeter/Sonar 等接管，改在应用内直接指定 `CABLE Output`。
   - 2026-09-07 用户实测：按住语音键期间 CABLE Output 电平波动，语音音频链路真机确认通过；识别端文字上屏取决于使用方工具及其触发键（当前注入 F13）配置。
3. 语音期间点「回眸」页勾选记录，结束后查看记录列表。
   - 预期：新增一条含时间与应用标题的记录（若目标应用文本可读）。
4. 开启「保存原始语音录音」重复步骤 2。
   - 预期：`Documents\SayAll Recordings\` 生成 16 kHz 单声道 WAV，能播放且时长与按住时间一致。
5. 连续 5 次按住/松开。
   - 预期：每次会话独立、无残留队列（松开后马上说第二句不串音）。

## 用例 5：按键映射（需真实遥控器）

1. 默认配置（未开自定义映射）：方向键/确定/返回按内置默认动作执行（光标移动/换行/退格等，单份动作——原生键盘事件在活跃期被钩子拦截，动作来自程序注入）；音量键调系统音量（consumer page 键系统直通）。
   - 失败判定：按键无反应，或动作重复（一次按键多次触发=原生与注入双份，属钩子拦截回归）。
2. 开启「启用自定义按键映射」，把「上」单击设为 `Esc`，「OK」设为 `Ctrl+Enter`。
   - 预期：遥控器按键产生对应动作；日志出现按键事件；原生方向键事件被吞掉（光标不移动）。
3. 长按「返回」。
   - 预期：按 550ms 后触发长按动作并以 50ms 间隔重复；松开停止。
4. 语音键在任何模式下都不应触发双击/长按动作，只按下开始、松开结束。

## 用例 6：本地 Agent 访问（MCP，可无遥控器）

1. 先用例 4/回眸生成至少一条记录（或手工向 `%APPDATA%\SayAll\transcripts.json` 写入测试数据）。
2. 控制台运行 `SayAll.exe --mcp-stdio`，逐行输入：
   - `{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}` → 返回 serverInfo。
   - `{"jsonrpc":"2.0","method":"notifications/initialized"}` → 无响应。
   - `{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}` → 包含 `transcript_search`。
   - `{"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"transcript_search","arguments":{"limit":5}}}` → content 内含记录 JSON。
   - 失败判定：任一步无响应、报 Parse error、或返回了音频/文件系统等越界数据。
3. 关闭 stdin（Ctrl+C）后进程退出。

## 用例 7：设置持久化与开机自启

1. 修改语言为 English → 界面与托盘文案切换为英文；重启后保持。
2. 勾选「登录时自动启动」→ `reg query HKCU\Software\Microsoft\Windows\CurrentVersion\Run /v SayAll` 存在；取消勾选后消失。
3. 退出应用（托盘菜单）后重新启动，所有设置保留。

## 用例 8：界面（侧边栏导航重构版，2026-09-05）

布局对齐 macOS 原版：左侧边栏导航（连接与语音 / 按键映射 / 统计 / 回眸 / 关于）+ 内容卡片。

1. 启动后默认显示「连接与语音」：状态卡（状态圆点 绿=已连接 / 橙=搜索中 / 红=失败或停止 + 重新连接按钮）、语音输出卡（设备下拉显示真实设备名，虚拟声卡带 ★）、语音键卡。
   - 失败判定：下拉显示 `{0.0.0.0.…}` 形式原始 Id、状态圆点颜色与连接状态不符。
2. 「按键映射」页：页头开关（iOS 风格 switch）、左列卡片顺序为 电源/上/左/返回/主页/菜单，右列为 语音键说明卡（"固定"标签，说明语音与 Fn 不参与自定义映射）/右/确定/下/音量+/音量−/TV；每张配置卡含单击/双击/长按三个下拉子卡；中央遥控器实物照片（原仓库 RC003 官方图，透明底与页面底色融合，无黑框）+ 灰色细引线从每张卡片连到照片上对应按键的位置（连通域检测校准）、底部条（映射状态 + 语音键模拟 Fn 点按 + 恢复默认）。
   - 点击任一下拉获得焦点 → 该卡片边框与对应引线变为蓝色；焦点离开后恢复灰色。
   - 修改任一动作下拉 → 重启后保留；「恢复默认」→ 全部回到内置默认映射。
   - 滚动或缩放窗口后引线仍正确连接卡片与按键锚点。
   - 失败判定：修改后不持久化、恢复默认后仍执行旧映射、引线错位到无关按键或照片出现黑色背景框、聚焦下拉后卡片与引线未变蓝。
3. 「统计」页：日/周/全部分段选择（影响汇总数字），按键次数（蓝）/语音时长（橙）最近 7 天柱状图，单次语音时长排行榜（由回眸记录推算，前 3 名橙色）。
   - 失败判定：柱状图日期缺失、排行榜出现非语音记录。
4. 语言切换 English → 全部页面文案（含页签、卡头、映射卡动作名）随语言刷新。
   - 失败判定：任一页面残留中文。

日志合规：上述页面操作产生的日志不得包含设备名/路径明文（只允许 name_present + 短哈希）。

## 稳定回归项

- 测试音在语音会话进行中必须被拒绝（返回失败提示而不是混音）。
- 增益为 0 dB 时输出与原始解码一致（音量合理，无削波）。
- 断开蓝牙适配器后应用不崩溃，托盘状态回到「重连中/搜索中」，恢复适配器后自动重连。
- 连续运行 30 分钟无内存异常增长、无日志爆炸（日志按 10 MiB 滚动）。

## 验证边界（交付时必须如实标注）

- 本会话已完成：构建验证、进程冒烟、日志与状态机验证（BLE Scanning、音频配置、MCP 协议代码走查）；2026-09-07 新增：SetupAPI 枚举命中（`HID POLL total=17 matched=1`）、钩子与 Raw Input 启动（`HID HOOK installed ok=1` + `RAWINPUT listener ok=1`）、BLE Ready 真机就绪。
- 未完成（需真实环境）：真实 RC003 遥控器的 Raw Input 按键路径（首次按语音键应出现 `HID DEVICE connected source=rawinput` + `HID VOICEKEY down`）、F5 不再触发前台应用刷新、微信输入法 Ctrl+Win 模式文字上屏、按键映射实机行为（用例 5）、VB-CABLE 实机路由（用例 2 部分依赖虚拟声卡安装）、多应用输入法实测、睡眠唤醒重连实测。在上述用例完成前，Windows 移植版不标记为功能完成。
